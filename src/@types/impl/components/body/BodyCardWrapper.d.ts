import * as React from 'react';
type Props = Readonly<{
    children: React.ReactNode;
    id: string;
}>;
export default function BodyCardWrapper({ children, id, }: Props): React.JSX.Element;
export {};
