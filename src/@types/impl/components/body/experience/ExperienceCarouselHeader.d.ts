import * as React from 'react';
type Props = Readonly<{
    timeframe: string;
    title: string;
}>;
export default function ExperienceCarouselHeader({ timeframe, title, }: Props): React.JSX.Element;
export {};
