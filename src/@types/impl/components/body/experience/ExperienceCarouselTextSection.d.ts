import * as React from 'react';
type Props = Readonly<{
    children: React.ReactNode;
    top?: boolean;
}>;
export default function ExperienceCarouselTextSection({ children, top, }: Props): React.JSX.Element;
export {};
